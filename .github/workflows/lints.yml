name: Lints

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lints:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
      with:
        node-version: 12
    - run: yarn
    - run: yarn lint